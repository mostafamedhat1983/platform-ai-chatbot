# ========================================
# Backend Service Account
# ========================================
# Service account for backend pods to access AWS services
# Links to IAM role via EKS Pod Identity (configured in Terraform)
# Must match namespace in Terraform Pod Identity association (default)

apiVersion: v1
kind: ServiceAccount
metadata:
  name: chatbot-backend-service-account
  namespace: default 